# プロジェクト作業規約

このファイルはリポジトリ全体に適用する。タスクを実行するすべてのエージェントは、
実装、調査、文書化、検証、コミットの全工程で以下を守ること。

## 1. `ROADMAP.md` を作業順序の正本とする

1. タスクは必ず [`ROADMAP.md`](ROADMAP.md) に記載された順に、上から進める。
2. 着手対象は、最初の未完了タスクとする。現在のタスクを完了する前に、
   後続タスクへ先回りして実装しない。
3. `ROADMAP.md` にない新規作業は、着手前に適切な実施位置へ未完了タスクとして
   登録する。登録しただけで既存の未完了タスクを飛ばして着手してはならない。
4. 後続タスクの前提になる準備も、現在のタスクに必要な最小範囲だけ行う。
5. タスクが阻害されている場合は完了扱いにせず、阻害要因を `docs/` に記録して
   ユーザーへ報告する。理由を残さずに後続タスクへ飛ばない。
6. 並行作業は、同じ現在タスクを明示的に分割し、変更範囲と依存関係が衝突しないと
   確認できた場合に限る。後続のロードマップ項目を並行して先行実装しない。

## 2. 着手前に必ず全体像とタスクの目的を把握する

コードを変更する前に、少なくとも次を確認する。

- `README.md`、`ROADMAP.md`、`FEATURE_MATRIX.md` を読み、製品目標、現在地、対象 Phase、
  アーキテクチャ上の境界、完了条件を確認する。
- リポジトリ全体の構成と、対象タスクに関係するコード、テスト、ビルド設定、既存の
  `docs/`、ADR を調べる。
- 現在の作業ツリーを確認し、ユーザーや他の作業者による既存変更を把握する。
  無関係な変更を上書き、削除、またはコミットしない。
- 対象タスクについて、目的、必要な成果物、対象範囲、対象外、依存関係、リスク、
  受け入れ条件、必要な検証を説明できる状態にする。
- 既存実装や文書だけで判断できない前提は、推測のまま固定せず、調査結果または
  明示した仮定として記録する。

着手時に、対象 Phase に対応する `docs/` 配下のタスクメモを新規作成または更新する。
タスク ID がある場合はファイル名に使用し、ない場合は内容を表す名前を付ける。
最初に目的、背景、範囲、対象外、依存関係、完了条件、検証方針を記載すること。

## 3. 判明事項と検討過程を `docs/` に逐次残す

タスク中に得た情報は、重要度や大きさにかかわらず、判明した時点でタスクメモへ
追記する。チャット、一時的な端末出力、エージェントの記憶だけに残してはならない。

最低限、次を記録する。

- 調査で確認した事実、再現条件、参照したコードや資料
- 前提、疑問、仮説、および確認結果
- 検討した選択肢、比較した利点と欠点、採用・不採用の判断と理由
- 実装中に判明した制約、境界条件、互換性上の注意点
- 失敗した試行、その原因、同じ失敗を避けるための情報
- 実行した検証、期待値、結果、未検証事項
- リスク、阻害要因、後続タスクへ引き継ぐ事項

メモは後から別のエージェントが判断を再現できる具体性を持たせる。必要に応じて日付、
対象環境、コマンド、関連ファイルを記す。一方で、秘密情報、認証情報、個人情報、
巨大な未整理ログは記録せず、再現に必要な要点と安全な参照先だけを残す。

## 4. 大きなタスクは分割し、残作業を必ず追跡する

一度に安全に実装・レビュー・検証できない、独立した成果物が複数ある、複数の層へ
大きくまたがる、または完了条件が曖昧になる場合は、タスクを実施可能な単位へ分割する。

分割を決めた時点で、実装を進める前に次を行う。

1. 分割後のすべてのサブタスクと実施順、依存関係、個別の完了条件を `docs/` に書く。
2. 元の項目の直下または本来の実施位置に、現在実施する分を含む全サブタスクを、
   順序付きの未完了チェック項目として `ROADMAP.md` に追記する。
3. 元タスクを親項目として残す場合は、全サブタスクが完了するまで親を完了にしない。
4. 各サブタスクを完了するたびに個別に検証、文書更新、進捗更新、コミットを行う。

タスク中に範囲外の必要作業を発見した場合も、口頭の TODO や `docs/` 内だけで済ませず、
実施漏れを防ぐため `ROADMAP.md` の適切な位置へ未完了タスクとして追加する。詳細は
`docs/` に置き、その新規タスクが順番上の対象になるまでは先行実装しない。

## 5. `ROADMAP.md` には進捗状態だけを残す

`ROADMAP.md` は作業日誌ではなく、未完了か完了かを示す計画表として扱う。

許可する更新は次に限定する。

- タスクの状態を `[ ]` から `[x]` に変更すること
- 分割または新たに必要と判明した未完了タスクを、正しい実施位置へ追加すること
- 後続タスクに必要な指示書への相対リンクと、「実施時に参照する」旨を追記すること
- 順序や依存関係を明確にするために必要な最小限のタスク文言を整えること

次の内容は `ROADMAP.md` に書かず、必ず `docs/` に置く。

- 実装した内容や変更ファイルの要約
- 調査結果、設計判断、検討過程、注意事項
- テスト結果、測定値、コマンド出力
- コミット情報、作業日誌、完了報告
- 後続作業の詳細な手順や受け入れ方法

後続タスクへ詳細な指示が必要な場合は、先に `docs/` に指示書を作成する。
`ROADMAP.md` には、タスク名、状態、その指示書へのリンク、および参照して実施する旨だけを
記載する。部分的に終わっただけのタスクを `[x]` にせず、未完了項目を削除して完了に
見せないこと。

## 6. 完了前に実装と記録を検証する

タスクは、次をすべて満たした場合にのみ完了とする。

- タスクの目的と受け入れ条件を満たしている。
- 必要な実装、テスト、文書が揃い、分割した残作業が追跡されている。
- 変更箇所に対応するテスト、静的解析、フォーマット確認、ビルド、必要な統合・性能検証を
  実行し、結果を `docs/` に記録している。
- 対象 Phase の終了条件と、性能・安全性・所有権・スレッド境界などのプロジェクト規約に
  反していない。
- 差分を見直し、デバッグ用の一時変更、秘密情報、不要な生成物、無関係な変更が
  含まれていない。
- 次のタスクを始めるために必要な情報が `docs/` から辿れる。

検証を実行できない、または失敗が残る場合は、その理由と影響を `docs/` に記録し、
タスクを完了扱いにしない。テストを通すためだけに受け入れ条件を弱めたり、既存テストを
根拠なく削除したりしない。

## 7. 実装完了後はタスクごとに必ずコミットする

1. 実装と検証を終え、`docs/` を更新してから `ROADMAP.md` の状態を更新する。
2. コミット前に作業ツリーとステージ済み差分を確認し、そのタスクに属する変更だけを
   含める。
3. 完了したタスクまたは分割したサブタスクごとに、必ず一つの完了コミットを作成する。
4. コミットメッセージには、`DT-012`、`Phase 1-2` などのタスク番号、タスク ID、
   順番を表す番号を絶対に含めない。
5. コミットメッセージは、番号に頼らず修正内容が分かる、短く具体的な文にする。
   例: `Separate debug and release build paths`
6. ユーザーから明示的に依頼されていない限り、既存コミットの amend、rebase、squash、
   履歴の書き換えを行わない。

Git リポジトリでない、権限がない、競合する既存変更があるなどの理由でコミットできない
場合は、成功したように扱わない。変更を失わない状態で停止し、理由と未実施のコミットを
ユーザーへ明確に報告する。コミットせずに次のロードマップタスクへ進まない。

## 8. タスク終了時の報告

ユーザーへの完了報告には、少なくとも次を簡潔に含める。

- 完了した内容
- 実行した検証と結果
- 作成・更新した `docs/` の場所
- コミット ID と、タスク番号を含まないコミットメッセージ
- 残課題、追加したロードマップ項目、または阻害要因
